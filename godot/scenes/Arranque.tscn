; La escena es a propósito un solo nodo: toda la interfaz la arma Arranque.gd
; en _ready(). Un .tscn escrito a mano es fácil de romper de maneras que Godot
; reporta mal, y esta pantalla tiene que funcionar justamente cuando el resto
; del proyecto todavía no.

[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/Arranque.gd" id="1_arranque"]

[node name="Arranque" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_arranque")
